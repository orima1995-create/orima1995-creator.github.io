---
import { getCollection } from 'astro:content';
import WatchPage from '../../layouts/WatchPage.astro';

const watches = await getCollection('watches');
const entry = watches.find((item) => item.data.slug === 'pierce-duofon');
if (!entry) throw new Error('Pierce Duofon content was not found');
---
<WatchPage watch={entry.data} />
